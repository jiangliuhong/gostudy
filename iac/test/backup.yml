---
- hosts: all
  tasks:
    - name: create folder
      file:
        path: "/root/test/{{name}}"
        state: directory
        mode: 755
    - name: download zip
      get_url:
        url: http://yum.idcos.com/cloudcmp/backup/cloud-cpg.5.9.0_20231028.tar.gz
        dest: "/root/test/{{name}}"
        mode: 755
        force: yes
        validate_certs: no